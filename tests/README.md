# Tests

Place your unit tests here.

For example, create a file like `test_payment_service.py` and write test cases
for functions in the `services/` folder (e.g. `calculate_ticket_price`,
`seat_class`, `is_seat_available`, `generate_ticket_id`).

You can run your tests with a framework such as `pytest` or Python's built-in
`unittest`.
